{
  "name": "tafreeman-profile",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub profile + portfolio landing (static, Babel-in-browser JSX).",
  "scripts": {
    "validate:jsx": "node scripts/validate-jsx.mjs",
    "validate:html": "html-validate *.html",
    "validate": "npm run validate:jsx && npm run validate:html",
    "test:e2e": "playwright test",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "esbuild": "0.25.0",
    "html-validate": "9.5.3"
  }
}
